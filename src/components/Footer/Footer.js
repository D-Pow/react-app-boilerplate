import React from 'react';
import PropTypes from 'prop-types';

function Footer(props) {
    return (
        <div>Footer</div>
    );
}

Footer.propTypes = {

};

Footer.defaultProps = {

};

export default Footer;
