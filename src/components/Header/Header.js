import React from 'react';
import PropTypes from 'prop-types';

function Header(props) {
    return (
        <div>Header</div>
    );
}

Header.propTypes = {

};

Header.defaultProps = {

};

export default Header;
